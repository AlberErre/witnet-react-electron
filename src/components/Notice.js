import React from "react";

const Notice = () => (
	<div>
		<h3 className="notice-container__subtitle">Create smart contracts with real power</h3>
		<p className="notice-small-text"> Notice:</p>
	    <p className="notice-small-text"> Front-end to evaluate whether the data between a smart contract <br /> and its source is the same or not.</p>
	</div>
);

export default Notice;